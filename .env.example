# Copy to .env and fill in to enable the contact form's reCAPTCHA widget
# (src/pages/contact.astro). Without it, the form still works — it just
# relies on the honeypot field alone.
#
# Register the site (and get this key) at:
# https://www.google.com/recaptcha/admin
PUBLIC_RECAPTCHA_SITE_KEY=
